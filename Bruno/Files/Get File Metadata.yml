info:
  name: Get File Metadata
  type: http
  seq: 49

http:
  method: GET
  url: "{{hostname}}/api/v1/files/{{file_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

