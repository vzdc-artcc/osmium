info:
  name: Delete File
  type: http
  seq: 53

http:
  method: DELETE
  url: "{{hostname}}/api/v1/files/{{file_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

