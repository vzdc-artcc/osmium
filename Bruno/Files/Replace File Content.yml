info:
  name: Replace File Content
  type: http
  seq: 52

http:
  method: PUT
  url: "{{hostname}}/api/v1/files/{{file_id}}/content?filename={{file_name}}"
  auth: inherit
  headers:
    Content-Type: application/octet-stream
  body: replaced file content from bruno

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

