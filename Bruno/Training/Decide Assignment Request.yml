info:
  name: Decide Assignment Request
  type: http
  seq: 26

http:
  method: PATCH
  url: "{{hostname}}/api/v1/training/assignment-requests/{{training_request_id}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "status": "APPROVED"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
