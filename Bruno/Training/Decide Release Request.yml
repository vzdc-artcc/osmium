info:
  name: Decide Release Request
  type: http
  seq: 29

http:
  method: PATCH
  url: "{{hostname}}/api/v1/training/trainer-release-requests/{{training_request_id}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "status": "DENIED"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
