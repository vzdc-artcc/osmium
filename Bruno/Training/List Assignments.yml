info:
  name: List Assignments
  type: http
  seq: 22

http:
  method: GET
  url: "{{hostname}}/api/v1/training/assignments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

