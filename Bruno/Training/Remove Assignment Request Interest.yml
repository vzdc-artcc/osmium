info:
  name: Remove Assignment Request Interest
  type: http
  seq: 31

http:
  method: DELETE
  url: "{{hostname}}/api/v1/training/assignment-requests/{{training_request_id}}/interest"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

