info:
  name: List Release Requests
  type: http
  seq: 27

http:
  method: GET
  url: "{{hostname}}/api/v1/training/trainer-release-requests"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

