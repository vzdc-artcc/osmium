info:
  name: List Assignment Requests
  type: http
  seq: 24

http:
  method: GET
  url: "{{hostname}}/api/v1/training/assignment-requests"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

