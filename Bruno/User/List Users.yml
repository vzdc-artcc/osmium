info:
  name: List Users
  type: http
  seq: 33

http:
  method: GET
  url: "{{hostname}}/api/v1/user?limit={{user_limit}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

