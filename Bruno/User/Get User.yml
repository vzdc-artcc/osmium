info:
  name: Get User
  type: http
  seq: 34

http:
  method: GET
  url: "{{hostname}}/api/v1/user/{{cid}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

