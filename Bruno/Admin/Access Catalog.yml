info:
  name: Access Catalog
  type: http
  seq: 9

http:
  method: GET
  url: "{{hostname}}/api/v1/admin/access/catalog"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

