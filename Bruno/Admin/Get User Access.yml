info:
  name: Get User Access
  type: http
  seq: 10

http:
  method: GET
  url: "{{hostname}}/api/v1/admin/users/{{cid}}/access"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

