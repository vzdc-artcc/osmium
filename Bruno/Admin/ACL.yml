info:
  name: ACL
  type: http
  seq: 8

http:
  method: GET
  url: "{{hostname}}/api/v1/admin/acl"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

