info:
  name: Delete Event
  type: http
  seq: 16

http:
  method: DELETE
  url: "{{hostname}}/api/v1/events/{{event_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

