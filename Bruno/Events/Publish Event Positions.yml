info:
  name: Publish Event Positions
  type: http
  seq: 21

http:
  method: POST
  url: "{{hostname}}/api/v1/events/{{event_id}}/positions/publish"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

