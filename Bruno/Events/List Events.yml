info:
  name: List Events
  type: http
  seq: 12

http:
  method: GET
  url: "{{hostname}}/api/v1/events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

