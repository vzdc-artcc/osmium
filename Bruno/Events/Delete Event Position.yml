info:
  name: Delete Event Position
  type: http
  seq: 20

http:
  method: DELETE
  url: "{{hostname}}/api/v1/events/{{event_id}}/positions/{{position_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

