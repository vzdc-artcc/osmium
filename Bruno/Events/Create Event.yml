info:
  name: Create Event
  type: http
  seq: 14

http:
  method: POST
  url: "{{hostname}}/api/v1/events"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "title": "Spring Event",
        "event_type": "HOME",
        "host": "ZDC Events",
        "description": "Test event",
        "starts_at": "2026-04-05T18:00:00Z",
        "ends_at": "2026-04-05T22:00:00Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
