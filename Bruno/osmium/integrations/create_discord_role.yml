info:
  name: create_discord_role
  type: http
  seq: 11
  tags:
    - integrations
    - admin

http:
  method: POST
  url: "{{baseUrl}}/api/v1/admin/integrations/discord/roles"
  body:
    type: json
    data: |-
      {
        "discord_config_id": "config_uuid",
        "name": "Event Ping",
        "role_id": "345678901234567890"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
