info:
  name: update_discord_category
  type: http
  seq: 15
  tags:
    - integrations
    - admin

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/admin/integrations/discord/categories/{{categoryId}}"
  body:
    type: json
    data: |-
      {
        "name": "Major Events",
        "category_id": "456789012345678901"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
