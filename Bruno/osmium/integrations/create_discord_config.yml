info:
  name: create_discord_config
  type: http
  seq: 6
  tags:
    - integrations
    - admin

http:
  method: POST
  url: "{{baseUrl}}/api/v1/admin/integrations/discord/configs"
  body:
    type: json
    data: |-
      {
        "name": "Main Guild",
        "guild_id": "123456789012345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
