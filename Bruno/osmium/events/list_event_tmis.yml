info:
  name: list_event_tmis
  type: http
  seq: 13
  tags:
    - events

http:
  method: GET
  url: "{{baseUrl}}/api/v1/events/{{eventId}}/tmis"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
