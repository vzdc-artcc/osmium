info:
  name: get_event_ops_plan
  type: http
  seq: 11
  tags:
    - events

http:
  method: GET
  url: "{{baseUrl}}/api/v1/events/{{eventId}}/ops-plan"
  auth: inherit

runtime:
  variables:
    - name: eventId
      value: 0617d698-4e98-46b9-a10f-452affbea6a4

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
