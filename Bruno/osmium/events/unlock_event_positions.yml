info:
  name: unlock_event_positions
  type: http
  seq: 20
  tags:
    - events

http:
  method: POST
  url: "{{baseUrl}}/api/v1/events/{{eventId}}/positions/unlock"
  auth: inherit

runtime:
  variables:
    - name: eventId
      value: 0617d698-4e98-46b9-a10f-452affbea6a4

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
