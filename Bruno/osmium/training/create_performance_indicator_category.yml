info:
  name: create_performance_indicator_category
  type: http
  seq: 43
  tags:
    - training
    - admin

http:
  method: POST
  url: "{{baseUrl}}/api/v1/admin/training/performance-indicators/categories"
  body:
    type: json
    data: |-
      {
        "template_id": "template_uuid",
        "name": "Coordination",
        "sort_order": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
