info:
  name: delete_performance_indicator_criteria
  type: http
  seq: 49
  tags:
    - training
    - admin

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/admin/training/performance-indicators/criteria/{{criteriaId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
