info:
  name: list_performance_indicator_categories
  type: http
  seq: 42
  tags:
    - training
    - admin

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/training/performance-indicators/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
