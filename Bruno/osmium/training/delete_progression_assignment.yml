info:
  name: delete_progression_assignment
  type: http
  seq: 52
  tags:
    - training
    - admin

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/admin/training/progression-assignments/{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
