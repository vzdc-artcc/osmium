info:
  name: create_performance_indicator_criteria
  type: http
  seq: 47
  tags:
    - training
    - admin

http:
  method: POST
  url: "{{baseUrl}}/api/v1/admin/training/performance-indicators/criteria"
  body:
    type: json
    data: |-
      {
        "category_id": "category_uuid",
        "name": "Completes handoff on time",
        "sort_order": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
