info:
  name: get_user_dossier
  type: http
  seq: 53
  tags:
    - training

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/{{cid}}/dossier"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
