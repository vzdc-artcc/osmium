info:
  name: list_progression_assignments
  type: http
  seq: 50
  tags:
    - training
    - admin

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/training/progression-assignments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
