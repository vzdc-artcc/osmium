info:
  name: create_performance_indicator_template
  type: http
  seq: 39
  tags:
    - training
    - admin

http:
  method: POST
  url: "{{baseUrl}}/api/v1/admin/training/performance-indicators/templates"
  body:
    type: json
    data: |-
      {
        "name": "Tower Rubric v2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
