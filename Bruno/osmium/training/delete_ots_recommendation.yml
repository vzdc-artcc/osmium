info:
  name: delete_ots_recommendation
  type: http
  seq: 11
  tags:
    - training

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/training/ots-recommendations/{{recommendationId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Response
    description: OTS recommendation deleted
    request:
      url: "{{baseUrl}}/api/v1/training/ots-recommendations/{{recommendationId}}"
      method: DELETE
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""
  - name: 400 Response
    description: Invalid request
    request:
      url: "{{baseUrl}}/api/v1/training/ots-recommendations/{{recommendationId}}"
      method: DELETE
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
  - name: 401 Response
    description: Not authorized
    request:
      url: "{{baseUrl}}/api/v1/training/ots-recommendations/{{recommendationId}}"
      method: DELETE
    response:
      status: 401
      statusText: Unauthorized
      body:
        type: text
        data: ""
