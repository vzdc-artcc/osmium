info:
  name: delete_progression
  type: http
  seq: 33
  tags:
    - training
    - admin

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/admin/training/progressions/{{progressionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
