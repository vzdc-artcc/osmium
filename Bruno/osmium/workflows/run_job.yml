info:
  name: run_job
  type: http
  seq: 23
  tags:
    - workflows
    - admin

http:
  method: POST
  url: "{{baseUrl}}/api/v1/admin/jobs/{{jobName}}/run"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
