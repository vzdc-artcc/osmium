info:
  name: list_jobs
  type: http
  seq: 21
  tags:
    - workflows
    - admin

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/jobs"
  headers:
    - name: X-Response-Timezone
      value: "{{responseTimezone}}"
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
