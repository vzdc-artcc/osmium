info:
  name: vatsim_login
  type: http
  seq: 4
  tags:
    - auth

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth/vatsim/login"
  params:
    - name: prompt
      value: ""
      type: query
      description: Optional OAuth prompt override
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 307 Response
    description: Redirects to VATSIM OAuth
    request:
      url: "{{baseUrl}}/api/v1/auth/vatsim/login"
      method: GET
      params:
        - name: prompt
          value: ""
          type: query
          description: Optional OAuth prompt override
          disabled: true
    response:
      status: 307
      statusText: Temporary Redirect
      body:
        type: text
        data: ""
