info:
  name: Login As User (DEV)
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth/login/as/:cid"
  params:
    - name: cid
      value: "10000010"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
