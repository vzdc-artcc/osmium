info:
  name: list_publication_categories
  type: http
  seq: 4
  tags:
    - publications

http:
  method: GET
  url: "{{baseUrl}}/api/v1/publications/categories"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
