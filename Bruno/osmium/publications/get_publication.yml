info:
  name: get_publication
  type: http
  seq: 3
  tags:
    - publications

http:
  method: GET
  url: "{{baseUrl}}/api/v1/publications/:publication_id"
  params:
    - name: publication_id
      value: ""
      type: path
      description: Publication ID
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
