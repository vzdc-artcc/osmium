info:
  name: list_publication_categories
  type: http
  seq: 35
  tags:
    - admin
    - publications

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/publications/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
