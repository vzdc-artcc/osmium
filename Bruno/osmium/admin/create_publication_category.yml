info:
  name: create_publication_category
  type: http
  seq: 36
  tags:
    - admin
    - publications

http:
  method: POST
  url: "{{baseUrl}}/api/v1/admin/publications/categories"
  auth: inherit
  body:
    mimeType: application/json
    text: |-
      {
        "key": "example-category",
        "name": "Example Category",
        "description": "",
        "sort_order": 0
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
