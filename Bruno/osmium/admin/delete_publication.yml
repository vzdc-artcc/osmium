info:
  name: delete_publication
  type: http
  seq: 34
  tags:
    - admin
    - publications

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/admin/publications/:publication_id"
  params:
    - name: publication_id
      value: ""
      type: path
      description: Publication ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
