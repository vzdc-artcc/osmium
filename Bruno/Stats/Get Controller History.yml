info:
  name: Get Controller History
  type: http
  seq: 39

http:
  method: GET
  url: "{{hostname}}/api/v1/stats/controller/{{cid}}/history?year={{stats_year}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

