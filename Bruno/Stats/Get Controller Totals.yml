info:
  name: Get Controller Totals
  type: http
  seq: 40

http:
  method: GET
  url: "{{hostname}}/api/v1/stats/controller/{{cid}}/totals"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

