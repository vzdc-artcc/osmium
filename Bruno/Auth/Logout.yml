info:
  name: Logout
  type: http
  seq: 7

http:
  method: POST
  url: "{{hostname}}/api/v1/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
