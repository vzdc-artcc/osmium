info:
  name: Get Session
  type: http
  seq: 3

http:
  method: GET
  url: "{{hostname}}/api/v1/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
