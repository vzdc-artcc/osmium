info:
  name: VATSIM Login
  type: http
  seq: 5

http:
  method: GET
  url: "{{hostname}}/api/v1/auth/vatsim/login"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
