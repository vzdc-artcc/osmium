info:
  name: Login As (DEV)
  type: http
  seq: 4

http:
  method: GET
  url: "{{hostname}}/api/v1/auth/login/as/:cid"
  params:
    - name: cid
      value: "10000001"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
